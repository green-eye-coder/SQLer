create virtual environment in cmd terminal:
conda create -p <venv_name> python==3.10 -y

activate virthual environment:
conda activate <venv_name>/
to deactivate <venv_name>

create file:
.env
app.py
sql.py

requirements.........
streamlit
google-generativeai
python-dotenv